# Knowledge

这里按三层维护知识，不是把所有讨论都直接塞进正式知识库。

## 三层结构

1. `candidates/`
   放候选条目，允许粗糙。
2. `reviews/`
   放审核文档，用中文动作标记去留。
3. `entries/`
   放正式知识，只保留确认过的高价值内容。

## 中文审核动作

- `保留`
- `丢弃`
- `合并:<候选ID>`
- `稍后`

## 原则

- 回退不删除历史
- 旧路径不等于废料
- 候选先筛选，再入正式知识库
